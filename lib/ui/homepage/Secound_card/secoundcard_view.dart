import 'package:flutter/material.dart';

class SecoundCard extends StatelessWidget {
  const SecoundCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
body: Center(
  child: Text('ข้อมูลจังหวัด'),
),
    );
  }
}
